# F1.12 - Repair organization creation

Production returned a generic creation error because create_organization_with_canonical_rbac was absent. The old resolver also recreated a temporary table on its second call in the same transaction.

Installs the internal resolver and atomic create RPC with a local JSON mapping. Permission counts stay 53/39/26/12. Only service_role can call creation. No existing organizations or memberships are backfilled.

Validation: 12 PGlite checks passed locally and in Codespace. Two creates against the real Supabase schema returned four roles each in a rolled-back transaction. Production migration applied; REST RPC returned expected P3110 for invalid input. Direct browser execution remains denied. No diagnostic rows persisted. Authenticated browser creation still needs user validation.
