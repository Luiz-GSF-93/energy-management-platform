# F1.13 - Organization user administration

Adds invitation/existing-user linking and organization-scoped name, affiliation and role editing in the UI. Internal/external classification now belongs to organization_members; legacy global values remain unchanged and existing membership classification starts null. Name overrides also belong to each membership. Role assignment cannot exceed the actor's permissions. Details and audit commit atomically.

Adds invite acceptance/password setup using the authenticated recipient identity. Supabase allowed redirect is https://app.expertenergy.com.br/auth/accept-invite. Invitation delivery to external recipients requires custom SMTP: this production project still uses Supabase built-in mail. No real invitations or passwords were used during tests.

Validation: 48 backend suites / 487 tests passed; backend build and frontend lint/build passed; 9 database isolation/audit checks passed. Migration 20260924_f1_13_membership_details.sql applied to Supabase successfully. Supabase docs: https://supabase.com/docs/guides/auth/auth-smtp

Ten service-level affiliation compensation tests were replaced by transactional RPC tests and database rollback/isolation tests because updates no longer mutate global profiles. Plans and real dashboard metrics are the following increments.
