# F1.4e — License Catalog Execution Results

## Baseline

Published diagnostic baseline:

`9e92a1cff5bebe843e1ae7e68741357153ac7f17`

## Execution

The F1.4 catalog guard was executed against the authoritative Supabase
database through the SQL Editor.

Result:

- execution completed successfully;
- no guard raised an exception;
- no rows were returned by the diagnostic;
- transaction ended with ROLLBACK;
- no schema or data mutation was authorized.

## Authoritative findings

`public.licenses` exists and currently contains zero rows.

Structural state:

- primary key: `licenses_pkey (id)`;
- `licenses.id`: text, NOT NULL, UUID-text default;
- `licenses.organization_id`: text, NOT NULL;
- `organizations.id`: text, NOT NULL;
- organization identifier types are compatible;
- no foreign key exists on licenses;
- no CHECK constraints exist;
- four indexes exist, including the PK index;
- non-unique indexes exist on organization_id, active and status;
- no additional UNIQUE constraint exists;
- RLS is enabled;
- FORCE RLS is disabled;
- zero RLS policies exist;
- zero user triggers exist.

The existing application source contains no discovered consumer of the
license persistence contract outside SQL/schema artifacts.

## Existing license contract

The authoritative table contains 17 columns and preserves:

- lifecycle fields: active, status;
- temporal fields: start_date, end_date, renewal_date;
- capability fields: document_management, advanced_analytics,
  report_generation, free_market_management;
- quota fields: documents_limit, documents_used, max_consumer_units.

## Cardinality finding

Neither `btree_gist` nor `btree_gin` is currently installed.

F1.4 will not silently install PostgreSQL extensions.

A global UNIQUE constraint on organization_id is rejected because it would
prevent historical licenses and conflict with the approved persistence model.

Concurrency-safe prevention of overlapping effective license periods remains
a separate design decision and must not be approximated by application-only
read-before-write logic.

## Migration boundary

Any subsequent migration must be additive and compatibility-preserving.

Potential integrity additions may include:

- organization foreign key;
- lifecycle invariants;
- temporal invariants.

RLS policies and concurrency/cardinality enforcement require explicit design
before DDL authorization.

No DDL is authorized by this checkpoint.
