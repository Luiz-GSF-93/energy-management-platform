/* Exercises the real wizard in an isolated DOM with an in-memory API. */
const assert=require('node:assert/strict'),fs=require('node:fs'),Module=require('node:module'),ts=require('typescript');
const {JSDOM}=require('jsdom');const dom=new JSDOM('<div id="root"></div>',{url:'https://test.invalid'});
for(const k of ['window','document','HTMLElement','HTMLInputElement','HTMLSelectElement','HTMLTextAreaElement','HTMLFormElement','FormData','Event','MouseEvent'])global[k]=dom.window[k];
global.IS_REACT_ACT_ENVIRONMENT=true;
const React=require('react'),{act}=React,{createRoot}=require('react-dom/client');
for(const ext of ['.ts','.tsx'])require.extensions[ext]=(mod,file)=>mod._compile(ts.transpileModule(fs.readFileSync(file,'utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,jsx:ts.JsxEmit.ReactJSX,target:ts.ScriptTarget.ES2020,esModuleInterop:true}}).outputText,file);
const {entryIssues,entryPayload}=require('../../backend/dist/modules/contracts/services/entry-drafts.js');
let rows=[],requests=0,failSave=false;const clone=v=>JSON.parse(JSON.stringify(v));
const apiRequest=async(url,options)=>{if(!options)return clone(rows);requests++;if(failSave)throw Error('Falha simulada: campos preservados');const b=options.body,old=rows.find(r=>url.endsWith('/'+r.id));const p=entryPayload(b.kind,b.customerId,b.payload),issues=[...entryIssues(b.kind,p),...b.deferredSteps.map(n=>'step:'+n)];const r={id:old?.id||'entry-'+requests,customer_id:b.customerId,kind:b.kind,payload:clone(p),issues,status:issues.length?'INCOMPLETE':'DRAFT',revision:(old?.revision||0)+1,deferred_steps:b.deferredSteps,updated_at:new Date().toISOString()};rows=[r,...rows.filter(x=>x.id!==r.id)];return clone(r);};
const ui={Button:({children,variant,...p})=>React.createElement('button',{type:'button','data-variant':variant,...p},children),Card:({title,children})=>React.createElement('section',null,React.createElement('h2',null,title),children),Alert:({children})=>React.createElement('div',{role:'alert'},children),Input:({label,...p})=>React.createElement('label',null,label,React.createElement('input',p))};
const original=Module._load;Module._load=function(name,parent,main){if(name==='@/app/components/ui')return ui;if(name==='@/app/providers')return {useAuth:()=>({hasPermission:()=>true})};if(name==='@/app/lib/api/client')return {apiRequest};return original.call(this,name,parent,main);};
const Wizard=require('../app/backoffice/contracts/EntryWizard.tsx').default;
const a='00000000-0000-4000-8000-000000000001';const props={customers:[{id:a,company_name:'Cliente de teste'}],units:[{id:a,customer_id:a,name:'Unidade de teste',consumer_unit_number:'1'}],filterCustomer:'',onDirty:()=>{},onActive:()=>{},onRegistered:()=>{},requestStart:fn=>fn()};
const root=createRoot(document.getElementById('root'));let checks=0;const ok=v=>{assert.ok(v);checks++;};const visible=e=>!e.closest('[hidden]');
const click=async(text)=>{const button=Array.from(document.querySelectorAll('button')).find(b=>b.textContent===text&&visible(b));assert.ok(button,'Button '+text);await act(async()=>button.click());};
const field=(label,hidden=false)=>{const l=Array.from(document.querySelectorAll('label')).find(e=>e.firstChild?.textContent===label&&(hidden||visible(e)));assert.ok(l,'Field '+label);return l.querySelector('input,select,textarea');};
const fill=async(label,value)=>{const e=field(label);const proto=e.tagName==='SELECT'?HTMLSelectElement.prototype:e.tagName==='TEXTAREA'?HTMLTextAreaElement.prototype:HTMLInputElement.prototype;await act(async()=>{Object.getOwnPropertyDescriptor(proto,'value').set.call(e,value);e.dispatchEvent(new Event(e.tagName==='SELECT'?'change':'input',{bubbles:true}));});};
(async()=>{try{
 await act(async()=>root.render(React.createElement(Wizard,props)));
 await click('Inserir novo');await fill('Cliente em operação',a);await fill('Tipo de cadastro','management');await click('Próximo');
 await fill('Número / referência da vigência *','Honorário teste');await click('Próximo');await click('Preencher depois e pular');await fill('Regras de aplicação e condições comerciais *','Regra preservada');await click('Próximo');await click('Salvar lançamento');
 ok(rows[0].status==='INCOMPLETE'&&rows[0].deferred_steps.includes(1));ok(rows[0].payload.fixedFeeMonthly===undefined);ok(document.body.textContent.includes('Preenchimento pendente'));
 await click('Continuar preenchimento');ok(field('Número / referência da vigência *').value==='Honorário teste');await fill('Início da vigência *','2026-01-01');await fill('Término da vigência *','2026-12-31');await click('Próximo');await fill('Honorário fixo mensal por unidade (R$) *','0');await click('Anterior');ok(field('Número / referência da vigência *').value==='Honorário teste');await click('Próximo');ok(field('Honorário fixo mensal por unidade (R$) *').value==='0');await click('Próximo');ok(field('Regras de aplicação e condições comerciais *').value==='Regra preservada');await click('Próximo');
 failSave=true;await click('Salvar lançamento');ok(document.body.textContent.includes('Falha simulada'));ok(field('Honorário fixo mensal por unidade (R$) *',true).value==='0');failSave=false;await click('Salvar lançamento');ok(rows[0].status==='DRAFT'&&rows[0].payload.fixedFeeMonthly===0&&rows[0].revision===2);ok(document.body.textContent.includes('Rascunho — aguarda revisão'));
 await click('Inserir novo');await fill('Cliente em operação',a);await click('Próximo');await fill('Unidade consumidora *',a);await fill('Número do contrato *','Fornecedor teste');await fill('Início da vigência *','2026-01-01');await fill('Término da vigência *','2026-12-31');await click('Próximo');await fill('Quantidade contratada (MWh) *','120');await fill('Preço único (R$/MWh)','200');await click('Próximo');await fill('Sazonalidade','BOTH');await fill('Regra contratual de sazonalidade','Conforme contrato');await click('Adicionar ano de sazonalidade');await fill('Volume anual (MWh) 1','120');await click('Preencher depois e pular');await click('Adicionar ano / período');await fill('Início do período 1','2026-01-01');await fill('Fim do período 1','2026-12-31');await fill('Preço do período 1 (R$/MWh)','250.5');await fill('Tipo de garantia','BANK_GUARANTEE');await fill('Valor da garantia (R$)','1000');await fill('Instituição da garantia','Instituição teste');await click('Próximo');await click('Próximo');await click('Salvar lançamento');
 const supply=rows.find(r=>r.kind==='supply');ok(supply.payload.annualPrices[0].pricePerMwh===250.5);ok(supply.payload.seasonalVolumes[0].annualVolumeMwh===120);ok(supply.payload.guaranteeInstitution==='Instituição teste');
 await click('Continuar preenchimento');await click('Próximo');await click('Próximo');ok(field('Regra contratual de sazonalidade').value==='Conforme contrato');ok(field('Volume anual (MWh) 1').value==='120');await click('Próximo');ok(field('Preço do período 1 (R$/MWh)').value==='250.5');ok(field('Instituição da garantia').value==='Instituição teste');
 console.log('Guided entry UI checks passed:',checks);
 }finally{await act(async()=>root.unmount());dom.window.close();}})().catch(e=>{console.error(e);process.exitCode=1;});
